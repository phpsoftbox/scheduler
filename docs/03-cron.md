# Cron выражения

Поддерживается классический формат из 5 полей:

```
минуты часы день_месяца месяц день_недели
```

Допустимые конструкции:
- `*` — любое значение
- `*/5` — шаг
- `1,15,30` — перечисление
- `1-5` — диапазон

День недели принимает значения `0-7`, где `0` и `7` — воскресенье.
